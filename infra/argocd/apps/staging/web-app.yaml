apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-app-staging
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: ripplemark
    env: staging
spec:
  project: default
  source:
    repoURL: https://github.com/kushal-sharma-works/ripplemark.git
    targetRevision: feat/implementation-and-ehancements
    path: infra/helm/web-app
    helm:
      valueFiles:
        - values.yaml
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ripplemark
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
