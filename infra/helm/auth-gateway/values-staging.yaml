replicaCount: 2
image:
  tag: staging
resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 800m
    memory: 1Gi
ingress:
  host: auth.staging.ripplemark.local
config:
  NODE_ENV: staging
