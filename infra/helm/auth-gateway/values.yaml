replicaCount: 2

image:
  repository: ghcr.io/kushal-sharma-works/ripplemark-auth-gateway
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: nginx
  host: auth.dev.ripplemark.local
  path: /

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 75

config:
  PORT: "3000"
  NODE_ENV: development

secret:
  sealedSecretName: auth-gateway-secrets

probes:
  livenessPath: /health
  readinessPath: /health

securityContext:
  runAsNonRoot: true
  runAsUser: 10001
  readOnlyRootFilesystem: true
